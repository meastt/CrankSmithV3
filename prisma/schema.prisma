// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model Component {
  id          String   @id @default(uuid())
  type        String   // Frame, Wheel, etc.
  name        String
  discipline  String   @default("multi") // gravel, road, mtb, multi
  disciplineTags String? // JSON string array for secondary discipline tags
  builderEligible Boolean @default(false) // True when component can appear in gravel builder
  interfaces  String   // JSON string of interface standards
  attributes  String   // JSON string of technical specs
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model User {
  id        String       @id // Clerk User ID
  email     String       @unique
  builds    SavedBuild[]
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt
}

model SavedBuild {
  id        String   @id @default(cuid())
  name      String
  parts     String   // JSON string of selected parts
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
